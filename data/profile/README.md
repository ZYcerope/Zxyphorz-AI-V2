# Profile & Seed Memory (Zxyphorz AI)

This folder stores small, human-editable identity and seed-memory files.

- `seed_facts.json` is loaded automatically and injected into every new session.
- You can add your own stable preferences here (language, nickname, timezone, etc.).

**Default identity**: `Zxyphorz AI`
